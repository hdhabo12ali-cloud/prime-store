# دليل مشروع Prime Store — شرح كل ملف

هذا الملف يشرح **كل ملف موجود بالمشروع**، وش وظيفته، ووين تعدّل لو تبي تغيّر شي. المشروع مقسوم لجزئين:
1. **prime-store-web** — الموقع + الباك إند (يترفع على Netlify)
2. **prime-store-desktop** — برنامج سطح المكتب (exe)

---

## 1) الموقع (prime-store-web)

### الجذر
| الملف | الوظيفة |
|---|---|
| `netlify.toml` | إعدادات Netlify: أي رابط يروح لأي Function (مثال: `/api/*` يروح لـ `public-api.js`). هنا تضيف أي مسار جديد. |
| `.env.example` | قالب فاضي لكل المتغيرات السرية المطلوبة (Supabase, Discord, Gemini, Google...). انسخه باسم `.env` للتشغيل المحلي. |
| `.gitignore` | الملفات اللي GitHub ما يرفعها (السرية، node_modules، ملفات exe الكبيرة). |
| `package.json` | قائمة المكتبات المطلوبة + أمر التشغيل المحلي (`npm start`). |
| `local-server.js` | سيرفر بسيط لتجربة الموقع على جهازك قبل الرفع (`npm start` يشغّله). |
| `README.md` | تعليمات التشغيل والرفع. |
| `supabase-migration-*.sql` | أوامر SQL تجهّز جداول قاعدة البيانات — تشغّلها مرة وحدة بس من Supabase. |

### `public/` — كل شي يشوفه الزائر مباشرة
| الملف | الوظيفة |
|---|---|
| `index.html` | الصفحة الرئيسية بالكامل (كل الأقسام: Home, Packages, Plans, Products...). |
| `app.js` | كل المنطق التفاعلي بالصفحة الرئيسية: الترجمة، عرض البيانات، تسجيل الدخول، إعلان التحديث. |
| `styles.css` | تنسيق كل شي بالموقع (الألوان، الأحجام، الحركات). |
| `assets/` | الصور والشعارات (الشعار، الفافيكون، صور الفريق). |
| `admin/index.html` + `admin/assets/admin.js` + `admin/assets/admin.css` | لوحة التحكم الإدارية بالكامل (`/admin`). |
| `docs/index.html` + `docs/assets/` | صفحة توثيق الـAPI للمشتركين (`/docs`). |
| `npm/gsap@.../` | مكتبة GSAP (تُستخدم لحركات وانميشن الموقع). |
| `css2/index.html` | ملف خطوط Google Fonts. |

### `netlify/functions/` — الباك إند (الكود اللي يشتغل بالسيرفر، مو بالمتصفح)
| الملف | الوظيفة |
|---|---|
| `public-api.js` | كل الطلبات العامة: `/api/bot/*` (بيانات للبوتات)، `/api/keys/verify` (التحقق من مفتاح API). |
| `admin-api.js` | كل عمليات لوحة التحكم: تسجيل دخول الأدمن عبر Discord، CRUD للباكجات/المنتجات/الخطط/الشعارات/مفاتيح الـAPI. |
| `user-auth.js` | تسجيل دخول **الأعضاء العاديين** (Discord + Google) — منفصل عن الأدمن. |
| `catalog-js.js` | يولّد ملف `catalog.js` اللي يحمّل بيانات المتجر بالصفحة الرئيسية. |
| `ai-chat.js` | المساعد الذكي (Gemini) بالموقع. |
| `lib/auth.js` | دوال مشتركة لتسجيل الدخول (بناء الكوكيز، توقيع الجلسات) — يستخدمها admin-api وuser-auth. |
| `lib/supabase.js` | يفتح الاتصال بقاعدة بيانات Supabase. |

### `netlify/edge-functions/`
| الملف | الوظيفة |
|---|---|
| `app-gate.js` | لو حطيت `DESKTOP_APP_SECRET`، يرفض أي طلب ما يجي من برنامج سطح المكتب (يحمي الموقع من الظهور بمتصفح عادي). |

---

## 2) برنامج سطح المكتب (prime-store-desktop)

| الملف | الوظيفة |
|---|---|
| `main.js` | قلب البرنامج: يفتح النافذة، يدير تبويب "المتجر" و"Runner"، يشغّل/يوقف البوتات، يتحقق من التحديثات. |
| `preload.js` | جسر آمن بين واجهة البرنامج وNode.js (يكشف بس الأوامر اللي البرنامج يحتاجها). |
| `app-shell.html` | واجهة البرنامج (التبويبات، قائمة البوتات، صندوق اللوقز). |
| `app-shell.css` | تنسيق واجهة البرنامج. |
| `app-shell.js` | منطق الواجهة: التبديل بين التبويبات، إضافة/تشغيل/إيقاف البوتات. |
| `offline.html` | صفحة تظهر لو الموقع ما فتح (مشكلة اتصال). |
| `assets/icon.ico` / `icon.png` | أيقونة البرنامج. |
| `package.json` | إعدادات البناء (electron-builder) + رابط GitHub للتحديث التلقائي. |

---

## أين أضيف شي جديد؟ (خريطة سريعة)

- **صفحة جديدة بالموقع؟** → أضف قسم بـ `public/index.html` + دوال عرضه بـ `public/app.js`
- **جدول جديد بقاعدة البيانات؟** → ملف SQL جديد + دوال CRUD بـ `admin-api.js`
- **مسار API جديد؟** → أضف الكود بـ `public-api.js` أو `admin-api.js`/`user-auth.js` + سطر Redirect بـ `netlify.toml`
- **ميزة جديدة بالبرنامج (exe)؟** → `main.js` (المنطق) + `app-shell.html/js/css` (الواجهة)
